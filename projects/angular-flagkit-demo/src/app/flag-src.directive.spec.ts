import { FlagSrcDirective } from './flag-src.directive';

describe('FlagSrcDirective', () => {
  it('should create an instance', () => {
    const directive = new FlagSrcDirective();
    expect(directive).toBeTruthy();
  });
});
